# Yerel yazı tipleri

Build sırasında Google Fonts indirilmez. Kaynak: Fontsource (SIL OFL).

- Inter — latin + latin-ext, variable
- Newsreader — latin + latin-ext, variable
- Manrope — latin + latin-ext, variable

`next/font/local` dosyayı layout’un yanından okur; kopyalar `apps/web/src/app/fonts` ve `apps/admin/src/app/fonts` altındadır.
